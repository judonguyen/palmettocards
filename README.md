# TestPages

My first web project. A simple page with HTML, CSS, and JavaScript.

## Files
- `index.html` — the page structure
- `style.css` — the styling
- `script.js` — the interactivity (click counter)

## Running locally
Open `index.html` in any web browser.
